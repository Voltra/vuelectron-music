export type AppColor = "bg" | "default" | "accent" | "secondary" | "white";

export interface AppTheme {
	// Colors
	bg: string;
	default: string;
	accent: string;
	secondary: string;
	white: string;
}
